{% extends 'MyexpCmsBundle:Admin:base.html.twig' %}
{% block title %}{{ 'slider.manage'|trans }}{{ parent() }}{% endblock %}
{% block breadcrumd %}
    <div class="active section">{{ 'slider.manage'|trans }}</div>
{% endblock %}
{% block content %}
    <table class="ui green table">
        <thead>
            <tr>
               <th>{{ 'slider.id'|trans }}</th>
                <th>{{ 'slider.name'|trans }}</th>
                <th>{{ 'slider.title'|trans }}</th>
                <th>{{ 'common.actions'|trans }}</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in pagination %}
            <tr>
                    <td>
                        <a href="{{ path('link_show', { 'id': entity.id }) }}">{{ entity.id }}</a>
                        <input type="hidden" name="ids[]" value="{{ entity.id }}" />
                    </td>
                    <td>{{ entity.trans.title }}</td>
                    <td><input type="text" name="paths[{{ entity.id }}]" value="{{ entity.path }}" /></td>
                    <td><input type="number" name="orders[{{ entity.id }}]" value="{{ entity.sortOrder }}" class="number" /></td>
                    <td class="actions">
                        <a href="{{ path('admin_slider_edit', { 'id': entity.id }) }}">{{ 'common.edit'|trans }}</a>
                    </td>
                </tr>
        {% endfor %}
        </tbody>
        <tfoot>
            <tr>
                <th colspan="8">
                    <div class="ui left small button">
                        Approve
                    </div>
                    <div class="ui left small button">
                        Approve All
                    </div>
                    <a class="ui left small teal labeled icon button" href="{{ path('admin_slider_new') }}">
                        <i class="archive icon"></i> {{ 'slider.new'|trans }}
                    </a>
                    {{ knp_pagination_render(pagination) }}
                </th>
            </tr>
        </tfoot>
    </table>
{% endblock %}
