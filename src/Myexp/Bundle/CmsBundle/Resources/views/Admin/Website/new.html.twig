{% extends 'MyexpCmsBundle:Admin:base.html.twig' %}
{% block breadcrumd %}
    <a class="section" href="{{ path('admin_website') }}">{{ 'website.manage'|trans }}</a>
    <i class="right angle icon divider"></i>
    <div class="active section">{{ 'website.new'|trans }}</div>
{% endblock %}
{% block content %}
    <h1>{{ 'website.new'|trans }}</h1>
    {{ form(form) }}
{% endblock %}
