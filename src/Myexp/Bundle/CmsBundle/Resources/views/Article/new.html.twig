{% extends 'SmtCmsBundle:Admin:layout.html.twig' %}
{% block title %}{{ 'article.new'|trans }}{% endblock %}
{% block breadcrumd %}
        <a href="{{ path('admin') }}">{{ 'nav.home'|trans }}</a> ><a href="{{path('article')}}">{{ 'article.manage'|trans }}</a> ><a href="{{app.request.getRequestUri()}}">{{ 'article.new'|trans }}</a></span>
{% endblock %}
{% block content %}
<form action="{{ path('article_create') }}" method="post" {{ form_enctype(form) }}>
    <div class="form">
        {% form_theme form 'SmtCmsBundle:Form:tabs.html.twig' %}
        {{ form_widget(form) }}
        <div class="form-actions">
            <input class="btn btn-primary" type="submit" value="{{ 'common.submit'|trans }}" />
            <input class="btn btn-danger" type="reset" value="{{ 'common.reset'|trans }}" />
        </div>
    </div>
</form>
<form action="{{ path('article') }}" method="post">
    <div class="form-actions">
        <input class="btn btn-primary" type="submit" value="{{ 'common.back_list'|trans }}"/>
    </div>
</form>
{% endblock %}
{% block javascripts %}
{{ parent() }}
<script type="text/javascript" src="{{ asset('ckeditor/ckeditor.js') }}"></script>
<script type="text/javascript" src="{{ asset('ckfinder/ckfinder.js') }}"></script>
<script type="text/javascript">
    $(document).ready(function() {
        $('.tabs').tabs();
        var editor_zh = CKEDITOR.replace('smt_cmsbundle_article_type_translations_zh_content');
        CKFinder.setupCKEditor(editor_zh, '/ckfinder/');
    });
</script>
{% endblock %}