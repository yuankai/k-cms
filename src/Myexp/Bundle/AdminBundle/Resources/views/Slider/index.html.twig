{% extends 'MyexpAdminBundle::base.html.twig' %}
{% block title %}{{ 'slider.list'|trans }}{{ parent() }}{% endblock %}
{% block breadcrumd %}
    <div class="active section">{{ 'slider.manage'|trans }}</div>
{% endblock %}
{% block content %}
    <table class="ui green table">
        <thead>
            <tr>
                <th>{{ 'slider.id'|trans }}</th>
                <th>{{ 'slider.name'|trans }}</th>
                <th>{{ 'slider.title'|trans }}</th>
                <th>{{ 'common.actions'|trans }}</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in pagination %}
                <tr>
                    <td>{{ entity.id }}</td>
                    <td>{{ entity.name }}</td>
                    <td>{{ entity.title }}</td>
                    <td>
                        <div class="ui icon buttons">
                            <a class="ui button" href="{{ path('admin_slider_edit', { 'id': entity.id }) }}" title="{{ 'common.edit'|trans }}">
                                <i class="edit icon"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
        <tfoot>
            <tr>
                <th colspan="8">
                    <button class="ui left small olive button">
                        {{ 'common.approve'|trans }}
                    </button>
                    <button class="ui left small red button">
                        {{ 'common.deny'|trans }}
                    </button>
                    <a class="ui left small teal labeled icon button" href="{{ path('admin_slider_new') }}">
                        <i class="archive icon"></i> {{ 'slider.new'|trans }}
                    </a>
                    {{ knp_pagination_render(pagination) }}
                </th>
            </tr>
        </tfoot>
    </table>
{% endblock %}
